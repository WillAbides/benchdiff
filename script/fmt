#!/bin/sh

set -e

CDPATH="" cd -- "$(dirname -- "$(dirname -- "$0")")"

make -s bin/gofumpt bin/handcrafted >/dev/null

git ls-files -o -c --exclude-standard '*.go' |
  bin/handcrafted |
  xargs bin/gofumpt -w -e -s
